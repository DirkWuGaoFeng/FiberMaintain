# 接口文档

> 版本：v2.1 | 日期：2026-07-21

---

## 1. 概述

### 1.1 协议说明

| 通信方向              | 协议                            | 说明                |
| --------------------- | ------------------------------- | ------------------- |
| 前端 → API Gateway   | REST (HTTP/HTTPS) + WebSocket   | JSON 格式           |
| API Gateway → 微服务 | gRPC                            | Protocol Buffers    |
| 微服务 → 微服务      | gRPC (Unary + Server Streaming) | 内部调用 + 事件订阅 |
| 设备 → 微服务        | gRPC                            | 性能/告警上报       |

### 1.2 通用约定

| 项目     | 说明                                                          |
| -------- | ------------------------------------------------------------- |
| 批量上限 | 通用 ≤200；告警 ≤50                                         |
| 部分失败 | 返回全部条目，不存在的标记`found=false` + `error_message` |
| 超时     | 单条 2s；批量 5s                                              |
| 认证     | JWT (Web/移动端) / API Key (第三方)                           |

---

## 2. REST API（API Gateway 对外）

### 2.1 单盘管理

| 方法   | URL                              | 说明              |
| ------ | -------------------------------- | ----------------- |
| POST   | /api/v1/boards                   | 创建单盘          |
| DELETE | /api/v1/boards/{board_id}        | 删除单盘（级联）  |
| GET    | /api/v1/boards/{board_id}        | 查询单盘          |
| POST   | /api/v1/boards/batch             | 批量查询（≤200） |
| GET    | /api/v1/boards/{board_id}/fibers | 单盘关联连纤      |

### 2.2 拓扑管理

| 方法   | URL                                      | 说明                   |
| ------ | ---------------------------------------- | ---------------------- |
| POST   | /api/v1/topology/fibers                  | 创建连纤               |
| DELETE | /api/v1/topology/fibers/{fiber_id}       | 删除连纤               |
| GET    | /api/v1/topology/fibers/{fiber_id}       | 查询连纤               |
| POST   | /api/v1/topology/fibers/batch            | 批量查询（≤200）      |
| GET    | /api/v1/topology/fibers/{fiber_id}/scene | 查询光纤场景（含源宿） |

### 2.3 性能管理

| 方法 | URL                         | 说明         |
| ---- | --------------------------- | ------------ |
| POST | /api/v1/performance/report  | 设备上报性能 |
| GET  | /api/v1/performance/current | 当前性能查询 |
| GET  | /api/v1/performance/history | 历史性能查询 |

### 2.4 告警管理

| 方法 | URL                    | 说明                 |
| ---- | ---------------------- | -------------------- |
| POST | /api/v1/alarms/report  | 设备上报告警（产生） |
| POST | /api/v1/alarms/clear   | 设备清除告警         |
| GET  | /api/v1/alarms/current | 当前告警查询         |

### 2.5 光纤维护

| 方法 | URL                                   | 说明                     |
| ---- | ------------------------------------- | ------------------------ |
| GET  | /api/v1/fibers/{fiber_id}/performance | 光纤性能                 |
| GET  | /api/v1/fibers/{fiber_id}/spanloss    | 光纤衰耗                 |
| GET  | /api/v1/fibers/colored                | 有颜色连纤（按颜色筛选） |
| GET  | /api/v1/fibers/colored/all            | 全部有颜色连纤           |
| GET  | /api/v1/fibers/stats/realtime         | 实时统计                 |
| GET  | /api/v1/fibers/stats/trend            | 趋势查询                 |

#### 2.5.1 趋势查询 `/api/v1/fibers/stats/trend`

**请求参数（Query）：**

| 参数       | 类型   | 必填 | 说明                     |
| ---------- | ------ | ---- | ------------------------ |
| start_time | string | 否   | 开始时间（ISO 8601格式） |
| end_time   | string | 否   | 结束时间（ISO 8601格式） |

**响应示例：**

```json
{
    "points": [
        {
            "timestamp": "2026-07-22T00:00:00",
            "red_count": 3,
            "yellow_count": 5,
            "total_colored": 8
        },
        {
            "timestamp": "2026-07-22T00:05:00",
            "red_count": 4,
            "yellow_count": 6,
            "total_colored": 10
        }
    ]
}
```

### 2.6 WebSocket

| URL                         | 说明                                         |
| --------------------------- | -------------------------------------------- |
| ws://host:8081/ws/v1/events | 实时事件推送（告警变更、颜色变更、趋势更新） |

---

## 3. gRPC 接口定义

### 3.1 公共定义（common.proto）

```protobuf
syntax = "proto3";
package fiber.common;

enum BoardType {
    BOARD_TYPE_UNSPECIFIED = 0;
    ACTIVE = 1;
    PASSIVE = 2;
}

enum AlarmLevel {
    ALARM_LEVEL_UNSPECIFIED = 0;
    CRITICAL = 1;
    MINOR = 2;
}

enum AlarmEventType {
    ALARM_EVENT_UNSPECIFIED = 0;
    ALARM_RAISED = 1;
    ALARM_CLEARED = 2;
}

enum FiberEventType {
    FIBER_EVENT_UNSPECIFIED = 0;
    FIBER_CREATED = 1;
    FIBER_DELETED = 2;
}

enum BoardEventType {
    BOARD_EVENT_UNSPECIFIED = 0;
    BOARD_CREATED = 1;
    BOARD_DELETED = 2;
}

enum FiberColor {
    COLOR_UNSPECIFIED = 0;
    GREEN = 1;
    RED = 2;
    YELLOW = 3;
}

message PortInfo {
    int32 port_id = 1;
    bool occupied = 2;
}

message BoardInfo {
    int32 board_id = 1;
    BoardType board_type = 2;
    int32 ne_id = 3;
    repeated PortInfo ports = 4;
}

message FiberInfo {
    int32 fiber_id = 1;
    int32 src_board_id = 2;
    int32 src_port_id = 3;
    int32 src_ne_id = 4;
    int32 dst_board_id = 5;
    int32 dst_port_id = 6;
    int32 dst_ne_id = 7;
    string created_at = 8;
}

message HealthCheckResponse {
    bool serving = 1;
    string version = 2;
}
```

---

### 3.2 BoardService（board.proto，8 RPC）

```protobuf
syntax = "proto3";
package fiber.board;
import "common.proto";

service BoardService {
    rpc CreateBoard(CreateBoardRequest) returns (CreateBoardResponse);
    rpc DeleteBoard(DeleteBoardRequest) returns (DeleteBoardResponse);
    rpc GetBoard(GetBoardRequest) returns (GetBoardResponse);
    rpc BatchGetBoards(BatchGetBoardsRequest) returns (BatchGetBoardsResponse);
    rpc GetBoardFibers(GetBoardFibersRequest) returns (GetBoardFibersResponse);
    rpc SubscribeBoardEvents(SubscribeBoardEventsRequest) returns (stream BoardEvent);
    rpc UpdatePortOccupied(UpdatePortOccupiedRequest) returns (UpdatePortOccupiedResponse);
    rpc HealthCheck(HealthCheckRequest) returns (fiber.common.HealthCheckResponse);
}

message CreateBoardRequest {
    int32 board_id = 1;
    fiber.common.BoardType board_type = 2;
    int32 ne_id = 3;
}

message CreateBoardResponse {
    bool success = 1;
    string message = 2;
}

message DeleteBoardRequest {
    int32 board_id = 1;
}

message DeleteBoardResponse {
    bool success = 1;
    string message = 2;
    repeated int32 deleted_fiber_ids = 3;  // 级联删除的连纤
}

message GetBoardRequest {
    int32 board_id = 1;
}

message GetBoardResponse {
    fiber.common.BoardInfo board = 1;
}

message BatchGetBoardsRequest {
    repeated int32 board_ids = 1;  // ≤200
}

message BatchGetBoardsResponse {
    repeated BoardResult results = 1;
}

message BoardResult {
    bool found = 1;
    fiber.common.BoardInfo board = 2;
    string error_message = 3;
}

message GetBoardFibersRequest {
    int32 board_id = 1;
}

message GetBoardFibersResponse {
    repeated fiber.common.FiberInfo fibers = 1;
}

message SubscribeBoardEventsRequest {}

message BoardEvent {
    fiber.common.BoardEventType event_type = 1;
    int32 board_id = 2;
    fiber.common.BoardType board_type = 3;
    int32 ne_id = 4;
    string timestamp = 5;
}

message UpdatePortOccupiedRequest {
    int32 board_id = 1;
    int32 port_id = 2;
    bool occupied = 3;
}

message UpdatePortOccupiedResponse {
    bool success = 1;
}

message HealthCheckRequest {}
```

---

### 3.3 TopologyService（topology.proto，8 RPC）

```protobuf
syntax = "proto3";
package fiber.topology;
import "common.proto";

service TopologyService {
    rpc CreateFiber(CreateFiberRequest) returns (CreateFiberResponse);
    rpc DeleteFiber(DeleteFiberRequest) returns (DeleteFiberResponse);
    rpc GetFiber(GetFiberRequest) returns (GetFiberResponse);
    rpc BatchGetFibers(BatchGetFibersRequest) returns (BatchGetFibersResponse);
    rpc GetFibersByPort(GetFibersByPortRequest) returns (GetFibersByPortResponse);
    rpc GetFiberScene(GetFiberSceneRequest) returns (GetFiberSceneResponse);
    rpc SubscribeFiberEvents(SubscribeFiberEventsRequest) returns (stream FiberEvent);
    rpc HealthCheck(HealthCheckRequest) returns (fiber.common.HealthCheckResponse);
}

message CreateFiberRequest {
    int32 src_board_id = 1;
    int32 src_port_id = 2;
    int32 dst_board_id = 3;
    int32 dst_port_id = 4;
}

message CreateFiberResponse {
    bool success = 1;
    int32 fiber_id = 2;
    string message = 3;
}

message DeleteFiberRequest {
    int32 fiber_id = 1;
}

message DeleteFiberResponse {
    bool success = 1;
    string message = 2;
}

message GetFiberRequest {
    int32 fiber_id = 1;
}

message GetFiberResponse {
    fiber.common.FiberInfo fiber = 1;
}

message BatchGetFibersRequest {
    repeated int32 fiber_ids = 1;  // ≤200
}

message BatchGetFibersResponse {
    repeated FiberResult results = 1;
}

message FiberResult {
    bool found = 1;
    fiber.common.FiberInfo fiber = 2;
    string error_message = 3;
}

// 双向匹配：查 src 或 dst
message GetFibersByPortRequest {
    int32 board_id = 1;
    int32 port_id = 2;
}

message GetFibersByPortResponse {
    repeated fiber.common.FiberInfo fibers = 1;
}

message SubscribeFiberEventsRequest {}

message FiberEvent {
    fiber.common.FiberEventType event_type = 1;
    int32 fiber_id = 2;
    string timestamp = 3;
}

message HealthCheckRequest {}
```

---

### 3.4 PerformanceService（performance.proto，6 RPC）

```protobuf
syntax = "proto3";
package fiber.performance;
import "common.proto";

service PerformanceService {
    rpc ReportPerformance(ReportPerformanceRequest) returns (ReportPerformanceResponse);
    rpc GetCurrentPerformance(GetCurrentPerformanceRequest) returns (GetCurrentPerformanceResponse);
    rpc GetHistoryPerformance(GetHistoryPerformanceRequest) returns (GetHistoryPerformanceResponse);
    rpc BatchGetCurrentPerformance(BatchGetCurrentPerformanceRequest) returns (BatchGetCurrentPerformanceResponse);
    rpc BatchGetHistoryPerformance(BatchGetHistoryPerformanceRequest) returns (BatchGetHistoryPerformanceResponse);
    rpc HealthCheck(HealthCheckRequest) returns (fiber.common.HealthCheckResponse);
}

message ReportPerformanceRequest {
    int32 board_id = 1;
    int32 port_id = 2;
    double oop_value = 3;    // dBm, 可选
    double iop_value = 4;    // dBm, 可选
}

message ReportPerformanceResponse {
    bool success = 1;
    string message = 2;
}

message GetCurrentPerformanceRequest {
    int32 board_id = 1;
    int32 port_id = 2;
}

message GetCurrentPerformanceResponse {
    int32 board_id = 1;
    int32 port_id = 2;
    double oop_value = 3;
    double iop_value = 4;
    string updated_at = 5;
}

message GetHistoryPerformanceRequest {
    int32 board_id = 1;
    int32 port_id = 2;
    string start_time = 3;
    string end_time = 4;
}

message GetHistoryPerformanceResponse {
    repeated PerformanceRecord records = 1;
}

message PerformanceRecord {
    string recorded_at = 1;
    double oop_value = 2;
    double iop_value = 3;
}

message BatchGetCurrentPerformanceRequest {
    repeated PortRef ports = 1;  // ≤200
}

message PortRef {
    int32 board_id = 1;
    int32 port_id = 2;
}

message BatchGetCurrentPerformanceResponse {
    repeated PerformanceResult results = 1;
}

message PerformanceResult {
    bool found = 1;
    int32 board_id = 2;
    int32 port_id = 3;
    double oop_value = 4;
    double iop_value = 5;
    string error_message = 6;
}

message BatchGetHistoryPerformanceRequest {
    repeated PortRef ports = 1;  // ≤200
    string start_time = 2;
    string end_time = 3;
}

message BatchGetHistoryPerformanceResponse {
    repeated PortHistoryResult results = 1;
}

message PortHistoryResult {
    int32 board_id = 1;
    int32 port_id = 2;
    repeated PerformanceRecord records = 3;
}

message HealthCheckRequest {}
```

---

### 3.5 AlarmService（alarm.proto，9 RPC）

```protobuf
syntax = "proto3";
package fiber.alarm;
import "common.proto";

service AlarmService {
    rpc ReportAlarm(ReportAlarmRequest) returns (ReportAlarmResponse);
    rpc ClearAlarm(ClearAlarmRequest) returns (ClearAlarmResponse);
    rpc GetCurrentAlarm(GetCurrentAlarmRequest) returns (GetCurrentAlarmResponse);
    rpc BatchGetCurrentAlarms(BatchGetCurrentAlarmsRequest) returns (BatchGetCurrentAlarmsResponse);
    rpc SubscribeAlarmEvents(SubscribeAlarmEventsRequest) returns (stream AlarmEvent);
    rpc CreatePullCall(CreatePullCallRequest) returns (CreatePullCallResponse);
    rpc GetPullCallResult(GetPullCallResultRequest) returns (GetPullCallResultResponse);
    rpc CancelPullCall(CancelPullCallRequest) returns (CancelPullCallResponse);
    rpc HealthCheck(HealthCheckRequest) returns (fiber.common.HealthCheckResponse);
}

message ReportAlarmRequest {
    int32 board_id = 1;
    int32 port_id = 2;
    fiber.common.AlarmLevel alarm_level = 3;
}

message ReportAlarmResponse {
    bool success = 1;
    string message = 2;
}

message ClearAlarmRequest {
    int32 board_id = 1;
    int32 port_id = 2;
    fiber.common.AlarmLevel alarm_level = 3;
}

message ClearAlarmResponse {
    bool success = 1;
    string message = 2;
}

message GetCurrentAlarmRequest {
    int32 board_id = 1;
    int32 port_id = 2;
}

message GetCurrentAlarmResponse {
    repeated AlarmRecord alarms = 1;
}

message AlarmRecord {
    int32 board_id = 1;
    int32 port_id = 2;
    fiber.common.AlarmLevel alarm_level = 3;
    string raised_at = 4;
}

message BatchGetCurrentAlarmsRequest {
    repeated PortRef ports = 1;  // ≤50
}

message PortRef {
    int32 board_id = 1;
    int32 port_id = 2;
}

message BatchGetCurrentAlarmsResponse {
    repeated PortAlarmResult results = 1;
}

message PortAlarmResult {
    int32 board_id = 1;
    int32 port_id = 2;
    repeated AlarmRecord alarms = 3;
}

message SubscribeAlarmEventsRequest {}

message AlarmEvent {
    fiber.common.AlarmEventType event_type = 1;
    int32 board_id = 2;
    int32 port_id = 3;
    fiber.common.AlarmLevel alarm_level = 4;
    string timestamp = 5;
}

message CreatePullCallRequest {
    repeated PortRef ports = 1;      // 空=全部
    bool include_history = 2;
}

message CreatePullCallResponse {
    string task_id = 1;
    string status = 2;  // "processing"
}

message GetPullCallResultRequest {
    string task_id = 1;
}

message GetPullCallResultResponse {
    string status = 1;  // "processing" | "completed" | "failed"
    repeated AlarmRecord data = 2;
}

message CancelPullCallRequest {
    string task_id = 1;
}

message CancelPullCallResponse {
    bool success = 1;
}

message HealthCheckRequest {}
```

---

### 3.6 FiberMaintService（fiber_maint.proto，12 RPC）

```protobuf
syntax = "proto3";
package fiber.maint;
import "common.proto";

service FiberMaintService {
    rpc GetFiberPerformance(GetFiberPerformanceRequest) returns (GetFiberPerformanceResponse);
    rpc BatchGetFiberPerformance(BatchGetFiberPerformanceRequest) returns (BatchGetFiberPerformanceResponse);
    rpc GetFiberHistoryPerformance(GetFiberHistoryPerformanceRequest) returns (GetFiberHistoryPerformanceResponse);
    rpc BatchGetFiberHistoryPerformance(BatchGetFiberHistoryPerformanceRequest) returns (BatchGetFiberHistoryPerformanceResponse);
    rpc GetFiberSpanloss(GetFiberSpanlossRequest) returns (GetFiberSpanlossResponse);
    rpc BatchGetFiberSpanloss(BatchGetFiberSpanlossRequest) returns (BatchGetFiberSpanlossResponse);
    rpc GetColoredFibers(GetColoredFibersRequest) returns (GetColoredFibersResponse);
    rpc GetAllColoredFibers(GetAllColoredFibersRequest) returns (GetAllColoredFibersResponse);
    rpc GetFiberStatsRealtime(GetFiberStatsRealtimeRequest) returns (GetFiberStatsRealtimeResponse);
    rpc GetFiberStatsTrend(GetFiberStatsTrendRequest) returns (GetFiberStatsTrendResponse);
    rpc SubscribeFiberColorEvents(SubscribeFiberColorEventsRequest) returns (stream FiberColorEvent);
    rpc HealthCheck(HealthCheckRequest) returns (fiber.common.HealthCheckResponse);
}

message GetFiberPerformanceRequest {
    int32 fiber_id = 1;
}

message GetFiberPerformanceResponse {
    int32 fiber_id = 1;
    double src_oop = 2;
    double dst_iop = 3;
}

message BatchGetFiberPerformanceRequest {
    repeated int32 fiber_ids = 1;  // ≤200
}

message BatchGetFiberPerformanceResponse {
    repeated FiberPerformanceResult results = 1;
}

message FiberPerformanceResult {
    bool found = 1;
    int32 fiber_id = 2;
    double src_oop = 3;
    double dst_iop = 4;
    string error_message = 5;
}

message GetFiberHistoryPerformanceRequest {
    int32 fiber_id = 1;
    string start_time = 2;
    string end_time = 3;
}

message GetFiberHistoryPerformanceResponse {
    repeated FiberPerformanceRecord records = 1;
}

message FiberPerformanceRecord {
    string recorded_at = 1;
    double src_oop = 2;
    double dst_iop = 3;
}

message BatchGetFiberHistoryPerformanceRequest {
    repeated int32 fiber_ids = 1;  // ≤200
    string start_time = 2;
    string end_time = 3;
}

message BatchGetFiberHistoryPerformanceResponse {
    repeated FiberHistoryResult results = 1;
}

message FiberHistoryResult {
    int32 fiber_id = 1;
    repeated FiberPerformanceRecord records = 2;
}

message GetFiberSpanlossRequest {
    int32 fiber_id = 1;
}

message GetFiberSpanlossResponse {
    int32 fiber_id = 1;
    double spanloss = 2;  // 缺失返回 0
}

message BatchGetFiberSpanlossRequest {
    repeated int32 fiber_ids = 1;  // ≤200
}

message BatchGetFiberSpanlossResponse {
    repeated SpanlossResult results = 1;
}

message SpanlossResult {
    bool found = 1;
    int32 fiber_id = 2;
    double spanloss = 3;
    string error_message = 4;
}

message GetColoredFibersRequest {
    fiber.common.FiberColor color = 1;  // RED 或 YELLOW
}

message GetColoredFibersResponse {
    repeated ColoredFiberInfo fibers = 1;
}

message ColoredFiberInfo {
    fiber.common.FiberInfo fiber = 1;
    fiber.common.FiberColor color = 2;
    int32 scene_type = 3;
    int32 scenario_case = 4;
}

message GetAllColoredFibersRequest {}

message GetAllColoredFibersResponse {
    repeated ColoredFiberInfo fibers = 1;
}

message GetFiberStatsRealtimeRequest {}

message GetFiberStatsRealtimeResponse {
    int32 red_count = 1;
    int32 yellow_count = 2;
    int32 total_colored = 3;
}

message GetFiberStatsTrendRequest {
    string start_time = 1;
    string end_time = 2;
}

message GetFiberStatsTrendResponse {
    repeated TrendPoint points = 1;
}

message TrendPoint {
    string timestamp = 1;
    int32 red_count = 2;
    int32 yellow_count = 3;
    int32 total_colored = 4;
}

message HealthCheckRequest {}
```

---

## 4. 错误码定义

### 4.1 gRPC Status Codes

| gRPC Code           | 值 | 场景                                   |
| ------------------- | -- | -------------------------------------- |
| OK                  | 0  | 成功                                   |
| INVALID_ARGUMENT    | 3  | 参数非法                               |
| DEADLINE_EXCEEDED   | 4  | 超时                                   |
| NOT_FOUND           | 5  | 资源不存在                             |
| ALREADY_EXISTS      | 6  | ID 冲突                                |
| FAILED_PRECONDITION | 9  | 业务校验失败（端口占用、用途不匹配等） |
| INTERNAL            | 13 | 内部错误                               |
| UNAVAILABLE         | 14 | 服务不可用                             |

### 4.2 REST API 错误响应

```json
{
    "error_code": "PORT_OCCUPIED",
    "message": "Port 1 of board 5 is already occupied",
    "details": {}
}
```

| HTTP Status | error_code            | 说明                    |
| ----------- | --------------------- | ----------------------- |
| 400         | INVALID_ARGUMENT      | 参数错误                |
| 400         | BATCH_LIMIT_EXCEEDED  | 批量超限（>200 或 >50） |
| 401         | UNAUTHORIZED          | 未认证                  |
| 404         | NOT_FOUND             | 资源不存在              |
| 409         | PORT_OCCUPIED         | 端口已占用              |
| 409         | ALREADY_EXISTS        | 资源已存在              |
| 422         | PORT_PURPOSE_MISMATCH | 端口用途不匹配          |
| 500         | INTERNAL_ERROR        | 服务内部错误            |
| 503         | SERVICE_UNAVAILABLE   | 服务不可用              |

---

## 5. WebSocket 事件格式

### 5.1 连接

```
ws://host:8081/ws/v1/events?token=<JWT>
```

### 5.2 订阅消息（客户端→服务端）

```json
{
    "action": "subscribe",
    "channels": ["alarm", "fiber_color", "fiber_stats"]
}
```

### 5.3 推送事件（服务端→客户端）

**告警变更：**

```json
{
    "channel": "alarm",
    "event_type": "ALARM_RAISED",
    "data": {
        "board_id": 2,
        "port_id": 1,
        "alarm_level": "CRITICAL",
        "timestamp": "2026-07-21T10:00:00Z"
    }
}
```

**颜色变更：**

```json
{
    "channel": "fiber_color",
    "event_type": "COLOR_CHANGED",
    "data": {
        "fiber_id": 1001,
        "old_color": "GREEN",
        "new_color": "RED",
        "scene_type": 1,
        "timestamp": "2026-07-21T10:00:01Z"
    }
}
```

**统计更新：**

```json
{
    "channel": "fiber_stats",
    "event_type": "STATS_UPDATED",
    "data": {
        "red_count": 5,
        "yellow_count": 12,
        "total_colored": 17,
        "timestamp": "2026-07-21T10:05:00Z"
    }
}
```

### 5.4 心跳

| 方向           | 消息                    | 间隔     |
| -------------- | ----------------------- | -------- |
| 客户端→服务端 | `{"action":"ping"}`   | 每 15s   |
| 服务端→客户端 | `{"action":"pong"}`   | 立即响应 |
| 超时判定       | 30s 无 pong → 断线重连 | —       |

---

## 6. 服务间调用矩阵

| 调用方 → 被调方  | BoardSvc                                              | TopologySvc                                                        | PerfSvc                                | AlarmSvc                                                   |
| ----------------- | ----------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------- | ---------------------------------------------------------- |
| API Gateway       | ✅                                                    | ✅                                                                 | ✅                                     | ✅                                                         |
| 设备              | —                                                    | —                                                                 | ✅ ReportPerformance                   | ✅ ReportAlarm/ClearAlarm                                  |
| TopologyService   | ✅ GetBoard, UpdatePortOccupied, SubscribeBoardEvents | —                                                                 | —                                     | —                                                         |
| BoardService      | —                                                    | ✅ GetBoardFibers, DeleteFiber                                     | —                                     | —                                                         |
| FiberMaintService | —                                                    | ✅ GetFiber, BatchGetFibers, GetFibersByPort, SubscribeFiberEvents | ✅ GetCurrentPerf, BatchGetCurrentPerf | ✅ SubscribeAlarmEvents, CreatePullCall, GetPullCallResult |

> FiberMaintService 颜色重算时**不调用** AlarmService.GetCurrentAlarm，改为读取本地告警缓存。

---

## 7. 批量查询部分失败响应示例

**请求：**

```json
POST /api/v1/topology/fibers/batch
{"fiber_ids": [1, 2, 999]}
```

**响应（200）：**

```json
{
    "results": [
        {"found": true, "fiber": {"fiber_id": 1, "src_board_id": 5, ...}},
        {"found": true, "fiber": {"fiber_id": 2, "src_board_id": 8, ...}},
        {"found": false, "fiber": null, "error_message": "fiber_id 999 not found"}
    ]
}
```

---

**— 接口文档 结束 —**
